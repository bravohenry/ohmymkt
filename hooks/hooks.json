{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ohmymkt_start_campaign",
        "hooks": [
          {
            "type": "command",
            "command": "bun run ${CLAUDE_PLUGIN_ROOT}/src/hooks/gate-check-cli.ts"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "ohmymkt_run_cycle",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "[DUAL-TRACK CHECK] Review the cycle output above. If visibility rises while quality does not improve, or if the decision is INTERVENE, warn the user about single-track drift and recommend rebalancing quality-track investment before the next scale cycle."
          }
        ]
      },
      {
        "matcher": "ohmymkt_incident",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "[P0 ESCALATION CHECK] If the incident above is severity P0, immediately recommend: 1) Rollback the last deployment 2) Notify all stakeholders 3) Schedule same-day postmortem 4) Pause all active campaigns until root cause is identified."
          }
        ]
      }
    ]
  }
}
